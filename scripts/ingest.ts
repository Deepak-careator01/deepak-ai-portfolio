/**
 * Content ingestion script (Phase 3+).
 *
 * Usage: bun run scripts/ingest.ts
 *
 * Reads files from src/content/, chunks text, generates embeddings,
 * and stores them in PostgreSQL via Drizzle ORM.
 */
console.info("Ingest script placeholder — implement in Phase 3.");
