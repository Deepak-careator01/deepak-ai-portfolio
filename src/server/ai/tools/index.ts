/**
 * LangGraph agent tools (Phase 4+).
 *
 * Planned tools:
 * - searchPortfolio — vector search over content
 * - getProject — fetch project by slug
 * - getExperience — return experience timeline
 * - navigateToSection — return section anchor for client navigation
 */
export {};
